VITE_BACKEND_URL=https://tu-backend-en-render.onrender.com

# Avatar Lab (herramienta interna, ver front/src/pages/AvatarLabPage.jsx).
# En build de prod la ruta /dev/avatar-lab no se registra salvo que esto sea "true".
VITE_ENABLE_DEV_TOOLS=false
# Opcional: base propia para el runtime WASM de MediaPipe, en vez del CDN de jsdelivr.
# VITE_MEDIAPIPE_WASM_BASE=
